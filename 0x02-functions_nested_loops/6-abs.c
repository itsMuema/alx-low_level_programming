#include "main.h"
/**
 *_abs - prints absolute value of integer
 *@i : the integer variable
 *
 *Return: i if absolute else -i
 */
int _abs(int i)
{
	if (i > 0)
	{
		return (i);
	}
	else
	{
		return (-i);
	}
	return (0);
}
